[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "nirnay-recovery-communication-agent"
version = "1.0.0"
description = "Nirnay Recovery Communication Agent"
readme = "README.md"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["."]
include = ["nirnay_recovery_communication_agent*"]
